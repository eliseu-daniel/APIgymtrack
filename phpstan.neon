includes:
    - vendor/larastan/larastan/extension.neon

parameters:

    paths:
        - app/

    level: 5

    excludePaths:
        - ./*/*/FileToBeExcluded.php

    ignoreErrors:
        - '#Unused variable \$[a-z]+#'
